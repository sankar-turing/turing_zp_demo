query Notifications ($count: Int, $offset: Int) {
    notifications (
        count: $count,
        offset: $offset
    ) {
        __typename
        notifications {
            id
            title
            body
            userId
            read
        }
        nextOffset
    }
}
